export enum baseErrors {
  NETWORK = "network_error",
  UNKNOWN = "unknown_error",
  UNAUTHORIZED = "UNAUTHORIZED",
  AUTHORIZATION_FAILED = "AUTHORIZATION_FAILED",
}

export type TErrorCodes = baseErrors;
